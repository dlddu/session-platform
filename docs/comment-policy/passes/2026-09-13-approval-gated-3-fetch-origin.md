# 15차 범위 재판정 — `e2e_approval_gated_3_approval_path_test.go` (2026-09-13)

> **이 패스의 판정 두 건은 16차가 뒤집었다** — 파일 헤더 문단과 `f3OriginBase` doc 의 평문 http
> 근거는 ②(저장소 문서)로 복원되는 사본이었다. 아래 표의 그 두 행은 유효한 판정이 아니다:
> [2026-09-14 16차 범위 재재판정](./2026-09-14-approval-gated-3-header-copy.md). 나머지 판정은 그대로다.

15차(2026-09-12)가 이 파일을 21줄로 닫은 지 하루 만에 같은 범위가 다시 열렸다. 파일이 바뀌어서가
아니라 **파일이 사는 것이 바뀌어서**다: 차단 요인 `FETCH-ORIGIN` 이 `deploy/fetch-origin.yaml` 실물
배포로 해소되면서, 그때까지 §「남은 미검증 분기」의 표 행이던 두 갈래가 이 파일의 단언이 됐다.
주석 21 → 61줄.

## 판정 절차

15차와 같다 — 각 주석에 대해 「이 줄을 지우면 무엇이 복원 불가능해지는가」를 묻고, 코드·문서·커밋
메시지 중 어느 하나에서 그대로 읽히면 사본(②·③)으로 보고 지운다.

## 증분 40줄의 판정

전부 **유지**다. 이유는 하나로 모인다 — 증분은 원본 배포가 생기면서 **처음 말할 수 있게 된 것**이지,
어딘가에 이미 있는 문장의 사본이 아니다.

| 증분 | 무엇을 복원 불가능하게 하는가 | 판정 |
| --- | --- | --- |
| 파일 헤더 (10줄) | 이 파일의 경계가 어디까지인지. 15차 시점의 헤더는 「두 갈래를 의도적으로 사지 않는다」였고 그 문장은 이제 **거짓**이다. 남은 잔여 한 절(본문 도달)이 왜 여전히 밖인지는 코드 어디에도 없다 — 공급자 대역의 `replyShape` 를 읽어야 나오는 사실이다 | 유지 |
| `f3OriginBase` doc (4줄) | **이 테스트가 왜 평문 http 주소를 골랐는지.** `deploy/fetch-origin.yaml` 헤더에도 평문 http 의 근거가 있으나 그쪽이 적는 것은 **배포 판정**(EXT 배제 조항이 걸리는 이유)이고, 여기가 적는 것은 **대상 URL 선택**(MCP 컨테이너에 사설 발급자를 넣을 자리가 없다)이다. 같은 사실의 다른 절반이라 ②가 아니다 | 유지 |
| `f3FetchBudget` · `f3FetchSettle` doc (5줄) | 두 상수가 왜 마커 예산과 별개인지. **승인 통지는 fetch 이전에 발행된다** — 그래서 마커는 호출 완료의 증거가 아니고, 그 사실이 없으면 다음 사람이 두 상수를 마커 예산으로 접어 넣는다 | 유지 |
| `f3DocPath` doc (4줄) | 세션 키가 **쿼리가 아니라 경로 세그먼트**인 이유. URL 이 프롬프트를 타고 가므로 확장될 문자를 싣지 않는다는 것이고, 코드는 `"/doc/" + sessionID` 라고만 말한다 | 유지 |
| `originHits` doc (5줄) | 이 관측이 **치환이 아니라는 근거**. 원본이 자기 방문자를 세고 `/access-log` 는 자기 자신을 기록하지 않는다 — 그래서 읽는 행위가 보고할 도착을 만들어 내지 못한다. 이 문단이 없으면 다음 판정이 이 헬퍼를 seam 후보로 오인한다 | 유지 |
| `eventuallyFetched` doc (2줄) | 왜 sleep 이 아니라 폴링인지 — CLI 왕복과 게이트웨이 폴 간격 어느 쪽도 고정 길이가 아니다 | 유지 |
| 단언 A 주석 (4줄) | 「결정 전 무도달」이 **더 이상 vacuous 하지 않은 이유**. 같은 테스트가 뒤에서 도착을 관측해 도달 가능성을 스스로 증명한다는 것이 대조군이고, 이것이 없으면 이 단언은 예전의 공허한 음성 단언과 구별되지 않는다 | 유지 |
| 단언 B 주석 (6줄) | 마커가 호출을 대신할 수 없다는 것, 그리고 「정확히 1회」를 첫 도착 직후가 아니라 **정착 창 뒤에 다시 재는** 이유(두 번째 호출은 첫 번째 뒤에 온다) | 유지 |

## 제거 0줄

15차가 이미 사본 84줄을 걷어낸 범위라, 이번 증분에서 걷을 것은 나오지 않았다. 15차가 지목한 새
형태 — **「사본이 저작 다음 날 낡았다」**(헤더가 `validateFetchTarget` 의 옛 계약을 전제로 적혀
있었다) — 는 이번에 반대 방향으로 닫혔다: 낡은 전제를 지우는 것이 아니라, 그 전제가 풀려서 생긴
새 사실을 헤더가 다시 적었다.

## 남는 것

이 파일의 주석이 다음에 낡을 자리는 하나다 — 헤더가 「본문 도달은 아직 밖」이라고 말하는 근거가
공급자 대역의 상수 응답이므로, **대역이 프롬프트를 반영하는 응답을 내게 되면** 그 문단과
`docs/test/e2e.md` §「남은 미검증 분기」의 해당 행이 함께 낡는다. 둘은 같은 슬라이스에서 움직인다.
