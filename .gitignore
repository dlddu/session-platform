# --- Go ---
control-plane/bin/
# `go build ./...` in data-plane/ drops the agent binary in the module root
data-plane/agent

# --- Node / web ---
web/node_modules/
web/dist/
web/tsconfig.tsbuildinfo
*.log

# --- Playwright (web e2e) ---
web/test-results/
web/playwright-report/
web/blob-report/
web/playwright/.cache/

# --- embedded SPA build output ---
# The placeholder control-plane/internal/static/dist/index.html is tracked so
# `go build` works; the real built assets (and regenerated index.html) are
# produced by `make build` and must not be committed.
control-plane/internal/static/dist/assets/

# --- editor / OS ---
.DS_Store
.idea/
.vscode/
