# Placeholder data plane image. Replace with the real session workload runtime
# (session agent + CRIU-capable base) once defined. See ./README.md.
FROM alpine:3.20
# TODO(data-plane): add the session agent that the control plane talks to, and
# a CRIU-compatible runtime so pods can be checkpointed/restored (AC-B*).
CMD ["sh", "-c", "echo 'data-plane placeholder — session workload not implemented' && sleep infinity"]
